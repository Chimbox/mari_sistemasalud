<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mx.edu.itson.mari_salud">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mari_salud">
        <activity android:name=".ui.como_sientes.MisEstadosActivity"></activity>
        <activity android:name=".ui.como_sientes.MeSientoActivity" />
        <activity android:name=".ui.profile.AjustesEstiloVidaActivity" />
        <activity android:name=".ui.diccionario.SeccionDiccionarioActivity" />
        <activity android:name=".ui.metas.ActivityEditorMetas" />
        <activity android:name=".RegisterActivity" />
        <activity
            android:name=".MenuActivity"
            android:label="@string/title_activity_menu" />
        <activity android:name=".WelcomeActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>